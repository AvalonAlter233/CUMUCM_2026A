# 论文增补：A 级论证插入指南

> 供论文手使用。给出**改哪个文件的哪一节、原文定位句、以及可直接粘贴的正文**。
> 全部数值由 `论证_收缩与含水率自洽性.py` 与 `论证_端面效应二维轴对称.py` 生成，可复现（见第 5 节）。
>
> - **增补 C**：收缩与含水率场的自洽性检验（附件二的收缩由什么驱动）
> - **增补 D**：端面效应的一维假设误差量化（二维轴对称对照）

---

## 1. 速览

| # | 文件 | 位置 | 操作 | 对应增补 |
|---|---|---|---|---|
| 1 | `问题四_论文内容.md` | §7 之后新增 §7.1 | 新增 | C |
| 2 | `问题四_论文内容.md` | §2 假设第 3 条 | 补一句指向 §7.1 | C |
| 3 | `问题三_论文内容.md` | §6 之下新增 §6.5 | 新增 | D |
| 4 | `问题三_论文内容.md` | §7 结论 | 补第 4 条 | D |
| 5 | `问题一_论文内容.md` | §9 | 整节替换 | D |

---

## 2. 增补 C：收缩与含水率场的自洽性检验

### 2.1 结论摘要（先看数）

问题四把附件二的实测半径 $R(t)$ 当作**外生输入**，含水率由扩散方程独立算出。
本项检验回答一个该假设未回答的问题：**半径能否由模型内部的水分场预测出来？**

对候选驱动量 $C_{\text{driver}}$，理想收缩律（长度不变、仅径向收缩、无孔隙）为

$$\left(\frac{R}{R_0}\right)^2=\frac{\phi+C_{\text{driver}}}{\phi+C_0},\qquad
\phi=\frac{\rho_w}{\rho_s},$$

反解得 $\phi(t)=\dfrac{sC_0-C_{\text{driver}}}{1-s}$，$s=(R/R_0)^2$。若驱动量选得正确，$\phi$ 应沿全程近似恒定。

**12 h 之后的检验结果（$\phi$ 的恒定性）：**

| 候选驱动量 | $\phi$ 加权均值 | 组内标准差 | 变异系数 | 三段均值极差 |
|---|---:|---:|---:|---:|
| 体积平均含水率 $\overline C$ | 1.1505 | 0.0460 | **4.00 %** | **18.2 %** |
| 外层半域平均 $\overline C_{\text{out}}$ | 1.1809 | 0.0345 | 2.93 % | 14.1 % |
| **表面重构含水率 $C_s$** | **1.3485** | **0.0022** | **0.161 %** | **0.305 %** |

**结论：附件二的收缩由表面含水率驱动，而非体积平均含水率。**

体积平均口径在 0–6 h 甚至给出 $\phi=-0.27$（范围 $-0.85\sim0.26$），物理上不可能——$\phi=\rho_w/\rho_s>0$ 恒成立。负值意味着 $(R/R_0)^2<\overline C/C_0$，即**半径收缩快于水分体积的减少**，在理想收缩框架下无法成立。

**分窗口 $\phi$（表面含水率驱动）：**

| 时段 | $\phi$ 均值 | 标准差 | 范围 |
|---|---:|---:|---|
| 0–6 h | 2.7857 | 1.8497 | [1.4844, 10.7385] ← 异常段 |
| 6–12 h | 1.3814 | 0.0334 | [1.3523, 1.4824] |
| 12–24 h | 1.3486 | 0.0033 | [1.3436, 1.3552] |
| 24–40 h | 1.3467 | 0.0016 | [1.3430, 1.3496] |
| 40–51.1 h | 1.3508 | 0.0009 | [1.3491, 1.3522] |

**三点解读：**

1. **6 h 之后理想收缩律成立**，$\phi\approx1.348$ 稳定到 0.16 %（组内标准差 0.0022）。取 $\phi=1.3485$ 对应等效干物质体积参数 $\rho_s=\rho_w/\phi\approx742\ \mathrm{kg/m^3}$。注意该值把孔隙效应并入，不等于真实固体密度。
2. **前 6 h 是异常段**，$\phi=2.79$ 且散布极大，说明该阶段收缩快于水分驱动的预测，存在水分流失之外的结构变化（或测量因素）。
3. **判别力来自 $\phi$ 的恒定性，不是拟合优度。** 由于 $R(t)$ 与任何含水率指标都随时间单调，任意单调模型都能得到高 $R^2$（例如线性 $R/R_0=0.329(C/C_0)+0.575$ 的 $R^2=0.957$）。理想收缩律是不含可调时间函数的固定非线性形式，$\phi$ 沿全程恒定才说明驱动量选对。

### 2.2 与问题四假设的关系（必须写清楚，否则会被误读）

问题四 §2 假设 3 说的是：给定 $R(t)$ 后，对**内部速度场**补充闭合 $v=r\dot R/R$，并明确声明"不是附件二能够唯一确定的内部速度场"。

本项检验**不推翻该假设**，它回答的是另一个问题：把收缩内生化（不再把 $R(t)$ 当外生输入）时，应当用哪个含水率作为驱动量。答案：必须用**表面含水率**，用体积平均会得到非物理结果。

### 2.3 插入位置（改动 1）

**文件**：`问题四_论文内容.md`，在 §7「物性变化与收缩效应的拆分」之后、§8「数值检验」之前，新增 §7.1。

```markdown
### 7.1 收缩与含水率场的自洽性检验

问题四把附件二的实测半径 $R(t)$ 作为外生输入，含水率由扩散方程独立算出。本节检验一个该处理未回答的问题：半径能否由模型内部的水分场复现。

在长度不变、仅径向收缩且材料无孔隙的假设下，干物质守恒给出 $\rho_d(t)R^2(t)=\mathrm{const}$，而总体积等于干物质体积与水分体积之和，于是

$$
\left(\frac{R}{R_0}\right)^2=\frac{\phi+C_{\mathrm{driver}}}{\phi+C_0},
\qquad
\phi=\frac{\rho_w}{\rho_s},
$$

其中 $\rho_s$ 为干物质密度，$C_{\mathrm{driver}}$ 是驱动收缩的那一个含水率指标。反解可得

$$
\phi(t)=\frac{sC_0-C_{\mathrm{driver}}}{1-s},
\qquad
s=\left(\frac{R}{R_0}\right)^2 .
$$

该式不含任何可调的时间函数，因此若驱动量选择正确，$\phi(t)$ 应沿全程近似为常数；若 $\phi$ 出现漂移或负值，则说明驱动量选错。负值在物理上不可能，因为 $\phi=\rho_w/\rho_s>0$ 恒成立。

本文检验三种候选驱动量：体积平均含水率 $\overline C$、外层半域平均 $\overline C_{\mathrm{out}}$（$\xi\in[0.5,1]$）、以及 §5.3 重构的表面含水率 $C_s$。表 10 给出 12 h 之后 $\phi$ 的恒定性比较。

### 表 10  理想收缩律下三种驱动量的 $\phi$ 恒定性（12 h 之后）

| 候选驱动量 | $\phi$ 加权均值 | 组内标准差 | 变异系数 | 三段均值极差 |
|---|---:|---:|---:|---:|
| 体积平均含水率 $\overline C$ | 1.1505 | 0.0460 | 4.00 % | 18.2 % |
| 外层半域平均 $\overline C_{\mathrm{out}}$ | 1.1809 | 0.0345 | 2.93 % | 14.1 % |
| **表面含水率 $C_s$** | **1.3485** | **0.0022** | **0.161 %** | **0.305 %** |

*注：三段指 12–24 h、24–40 h、40–51.1 h；极差为三段 $\phi$ 均值之差。*

表面含水率口径使 $\phi$ 稳定到 0.16 %（组内标准差 0.0022），而体积平均口径的变异系数为 4.00 %，三段均值极差达 18.2 %。更关键的是，体积平均口径在 0–6 h 给出 $\phi=-0.27$（范围 $-0.85\sim0.26$），出现负值。负值意味着 $(R/R_0)^2<\overline C/C_0$，即半径收缩快于水分体积的减少，在理想收缩框架下不可能成立。**因此附件二的收缩由表面含水率驱动，而不是体积平均含水率。**

分时段的 $\phi$（表面含水率驱动）见表 11。

### 表 11  表面含水率驱动下 $\phi$ 的分时段取值

| 时段 | $\phi$ 均值 | 标准差 | $\phi$ 范围 |
|---|---:|---:|---|
| 0–6 h | 2.7857 | 1.8497 | [1.4844, 10.7385] |
| 6–12 h | 1.3814 | 0.0334 | [1.3523, 1.4824] |
| 12–24 h | 1.3486 | 0.0033 | [1.3436, 1.3552] |
| 24–40 h | 1.3467 | 0.0016 | [1.3430, 1.3496] |
| 40–51.1 h | 1.3508 | 0.0009 | [1.3491, 1.3522] |

6 h 之后理想收缩律成立，$\phi$ 稳定在 1.348 附近，对应等效干物质体积参数 $\rho_s=\rho_w/\phi\approx742\ \mathrm{kg/m^3}$。需要说明的是，该值是在"材料无孔隙"前提下反解得到的等效参数，把孔隙效应一并并入，不等于真实固体密度。

**前 6 h 是异常段**：$\phi$ 达 2.79 且散布极大（范围上限 10.74），说明该阶段的收缩快于水分流失所对应的量，存在水分流失之外的结构变化或测量因素。本文不进一步推断其具体机制，题面未提供该阶段的结构观测数据。

本检验对模型的含义是：附件二的收缩数据确实由水分流失控制，这支持将其作为传质问题的边界输入；但驱动量必须是**表面含水率**，而不是体积平均含水率。若后续工作要把收缩内生化（不再把 $R(t)$ 当作外生输入），应以上述 $\phi$ 与表面含水率为基础，而不是采用"半径随体积平均含水率线性变化"的简化。

需要注意，$R(t)$ 与任何含水率指标都随时间单调，故任意单调模型都能得到较高的拟合优度（例如以 $\overline C$ 为自变量的线性式 $R/R_0=0.329\,(\overline C/C_0)+0.575$ 的 $R^2=0.957$）。因此判别力并不来自 $R^2$，而来自 $\phi$ 的恒定性：理想收缩律是不含可调时间函数的固定非线性形式，只有驱动量选对，$\phi$ 才能沿全程保持不变。
```

### 2.4 连带改动（改动 2）

**文件**：`问题四_论文内容.md` §2 假设第 3 条，在末尾补一句。

原文：

```markdown
3. 内部各材料点按相同比例径向移动，材料速度为 $v(r,t)=r\dot R(t)/R(t)$。该式是根据边界运动补充的闭合假设，不是附件二能够唯一确定的内部速度场。
```

改为：

```markdown
3. 内部各材料点按相同比例径向移动，材料速度为 $v(r,t)=r\dot R(t)/R(t)$。该式是根据边界运动补充的闭合假设，不是附件二能够唯一确定的内部速度场。该假设只约束内部速度场；附件二的半径数据本身能否由水分场复现，见 §7.1。
```

---

## 3. 增补 D：端面效应的一维假设误差量化

### 3.1 方法

问题一至问题三采用圆柱中部截面的一维径向模型，隐含假设端面传热传质可忽略。题面未禁止端面作用：端面面积占全部外表面积 $R/L=0.08$，约 7.41 %。

本项检验建立二维轴对称 $(r,z)$ 有限体积求解器，在同一网格、同一物理设置下比较两种端面情形：

- **情形 A**：端面绝热绝湿（零通量）——理论上应严格退化为一维模型，用作实现校验；
- **情形 B**：端面与侧面处于相同环境、采用相同 $(h,h_m)$——端面效应的敏感性上界。

A 与 B 的达标时间之差即为端面效应的影响尺度。

**计算设置**：$N_r\times N_z=40\times250$ 单元，$\Delta r=\Delta z=0.05\ \mathrm{cm}$，计算半长 $L/2=12.5\ \mathrm{cm}$，时间步长 $\Delta t=30\ \mathrm{s}$，物理设置与问题三一致（固定半径 $R=2\ \mathrm{cm}$、附录三变物性、烘房边界 4 h 后取末 1 h 均值）。

### 3.2 结果

### 表 12  端面效应二维轴对称对照

| 情形 | 端面条件 | 连续临界时间 $t^*$ / h | 相对基准 |
|---|---|---:|---:|
| A | 绝热绝湿 | 57.9289 | 基准 |
| B | 与侧面同环境 | 57.9268 | $-7.6\ \mathrm s$（$-0.0037\%$） |

### 表 13  端面暴露引起的全域最大含水率偏差（情形 B 相对情形 A 降低）

| 量 | 数值 |
|---|---:|
| $\lvert C_{\max,A}-C_{\max,B}\rvert$ 最大值 | $2.23\times10^{-5}\ \mathrm{kg/kg}$（$t=3.02\ \mathrm h$，相对 $0.00127\%$） |
| 全程平均值 | $4.43\times10^{-6}\ \mathrm{kg/kg}$ |
| 达标时刻 | $2.19\times10^{-6}\ \mathrm{kg/kg}$ |
| 中心温度最大偏差 | $1.7\times10^{-3}\ \mathrm{K}$ |

**结论：端面效应使达标时间改变约 $7.6\ \mathrm s$，即 $0.0037\%$。**

作为尺度对照，同一组计算中径向网格误差（情形 A 相对一维高分辨率结果 $57.5224\ \mathrm h$）为 $+1463.5\ \mathrm s$（$+0.707\%$）。**端面效应比网格离散误差小两个数量级**，在本文网格精度下无法与离散误差分离，说明一维径向假设在本题几何条件下是可接受的。

### 3.3 实现校验与机理解释

**实现校验**（三重）：

1. **轴向均匀性**：情形 A 的场在轴向严格均匀，$\max_j\lvert C_j-C_0\rvert=1.02\times10^{-14}$，确认绝热端面精确退化为一维；
2. **与一维剖面一致**：$t=3600\ \mathrm s$ 时情形 A 的径向剖面与一维结果中心值相差 $0.0018$、表面值相差 $0.033$，差异来自径向网格（二维 $\Delta r=0.05\ \mathrm{cm}$ 对一维 $0.00625\ \mathrm{cm}$）；
3. **端面边界确实生效**：排除"端面条件未接入"这一可能。$t=600\ \mathrm s$ 时两种情形下表面附近单元含水率分别为

| $t=600\ \mathrm s$ | 端面外角 $(r{=}R,\ z{=}L/2)$ | 端面轴心 $(r{=}0,\ z{=}L/2)$ | 中部轴心 $(r{=}0,\ z{=}0)$ |
|---|---:|---:|---:|
| 情形 A（绝热） | 2.002291 | 2.550000 | 2.550000 |
| 情形 B（暴露） | 1.591065 | 2.024961 | 2.550000 |

端面在实实在在地干燥（端面轴心由 2.550 降至 2.025），但**控制达标的中部轴心完全不受影响**。

**机理**：水分自端面的渗透深度约为 $\sqrt{\int D\,\mathrm dt}\approx2\ \mathrm{cm}$，而计算半长为 $12.5\ \mathrm{cm}$。由最大值原理，全域最大含水率恒位于中部轴心，而该点距两端各 $12.5\ \mathrm{cm}$，远在端面影响范围之外。因此题面提示的 7.41 % 端面面积并不构成有效干燥面积——它能干燥端部区域，但控制不了最慢点。

### 3.4 插入位置（改动 3）

**文件**：`问题三_论文内容.md`，在 §6「数值验证、守恒性检验与情景敏感性评估」之下新增 §6.5。

```markdown
### 6.5 端面效应二维轴对称对照

前四节的一维径向模型隐含假设圆柱端面的传热传质可以忽略。题面未禁止端面作用：本问几何下端面面积占全部外表面积的 $R/L=0.08$，约 7.41 %。为量化该假设的误差，本节建立二维轴对称 $(r,z)$ 有限体积模型，在同一网格与同一物理设置下比较两种端面情形：情形 A 取端面绝热绝湿（零通量），情形 B 取端面与侧面处于相同环境、采用相同 $(h,h_m)$，后者构成端面效应的敏感性上界。

计算取 $N_r\times N_z=40\times250$ 单元（$\Delta r=\Delta z=0.05\ \mathrm{cm}$）、计算半长 $L/2=12.5\ \mathrm{cm}$、时间步长 $\Delta t=30\ \mathrm{s}$。表 12 给出连续临界时间，表 13 给出端面暴露引起的全域最大含水率偏差。

### 表 12  端面效应二维轴对称对照

| 情形 | 端面条件 | 连续临界时间 $t^*$ / h | 相对基准 |
|---|---|---:|---:|
| A | 绝热绝湿 | 57.9289 | 基准 |
| B | 与侧面同环境 | 57.9268 | $-7.6\ \mathrm s$（$-0.0037\%$） |

### 表 13  端面暴露引起的全域最大含水率偏差

| 量 | 数值 |
|---|---:|
| $\lvert C_{\max,A}-C_{\max,B}\rvert$ 最大值 | $2.23\times10^{-5}\ \mathrm{kg/kg}$（$t=3.02\ \mathrm h$，相对 $0.00127\%$） |
| 全程平均值 | $4.43\times10^{-6}\ \mathrm{kg/kg}$ |
| 达标时刻 | $2.19\times10^{-6}\ \mathrm{kg/kg}$ |
| 中心温度最大偏差 | $1.7\times10^{-3}\ \mathrm{K}$ |

端面效应使达标时间改变约 $7.6\ \mathrm s$，即 $0.0037\%$。作为尺度对照，同一组计算中径向网格误差（情形 A 相对一维高分辨率结果 $57.5224\ \mathrm h$）为 $+1463.5\ \mathrm s$（$+0.707\%$）。端面效应比网格离散误差小两个数量级，在本文网格精度下无法与离散误差分离。**因此一维径向假设在本题几何条件下是可接受的。**

该结论有两点实现层面的支持。其一，情形 A 的场在轴向严格均匀，$\max_j\lvert C_j-C_0\rvert=1.02\times10^{-14}$，确认绝热端面精确退化为一维；在 $t=3600\ \mathrm s$ 处其径向剖面与一维结果的中心值相差 $0.0018$、表面值相差 $0.033$，差异来自径向网格（二维 $\Delta r=0.05\ \mathrm{cm}$ 对一维 $0.00625\ \mathrm{cm}$）。其二，端面边界确实参与传质：$t=600\ \mathrm s$ 时情形 B 的端面轴心含水率已由初值 2.550 降至 2.025，而中部轴心仍为 2.550，与情形 A 完全相同。

机理解释如下：水分自端面的渗透深度约为 $\sqrt{\int D\,\mathrm dt}\approx2\ \mathrm{cm}$，而计算半长为 $12.5\ \mathrm{cm}$。由 §4.5 的最大值原理，全域最大含水率恒位于中部轴心，该点距两端各 $12.5\ \mathrm{cm}$，远在端面影响范围之外。因此题面提示的 7.41 % 端面面积并不构成有效干燥面积——它能够干燥端部区域，但控制不了决定达标的最慢点。
```

### 3.5 连带改动（改动 4、5）

**`问题三_论文内容.md` §7 结论**，补一条：

```markdown
4. **端面效应**：二维轴对称对照表明端面作用使达标时间改变约 $7.6\ \mathrm{s}$（$0.0037\%$），比径向网格误差 $1463.5\ \mathrm{s}$（$0.707\%$）小两个数量级，一维径向假设在本题几何条件下是可接受的（§6.5）；
```

**`问题一_论文内容.md` §9「一维假设的后续检验」**：该节现在写的是"可建立二维轴对称模型"，属于未兑现的承诺。整节替换为：

```markdown
## 9. 一维假设的检验

一维模型给出的是圆柱中部截面结果。本文进一步建立了 $T(r,z,t)$、$C(r,z,t)$ 的二维轴对称模型，在相同网格与物理设置下比较端面绝热绝湿与端面暴露两种情形，量化端面效应对达标时间的影响：端面效应约为 $7.6\ \mathrm{s}$（$0.0037\%$），比径向网格误差小两个数量级，详见问题三 §6.5。因此本文不再把端面效应留作待验证项。与之配套，问题二 §9.3 中原列的二维检验计划由该计算替代。
```

---

## 4. 图表清单

| 表号 | 内容 | 放置位置 |
|---|---|---|
| 表 10 | 理想收缩律下三种驱动量的 $\phi$ 恒定性 | 问题四 §7.1 |
| 表 11 | 表面含水率驱动下 $\phi$ 的分时段取值 | 问题四 §7.1 |
| 表 12 | 端面效应二维轴对称对照 | 问题三 §6.5 |
| 表 13 | 端面暴露引起的全域最大含水率偏差 | 问题三 §6.5 |

问题三 §6.5 正文中还有一个"$t=600\ \mathrm s$ 表面附近单元含水率"的小表（端面外角／端面轴心／中部轴心，两种情形对照），用于证明端面边界确实生效。它也可以并进表 13，由论文手按表序安排。

无需新增图片。`论证_端面效应二维轴对称.py` 的 JSON 中包含 $C_{\max}(t)$ 轨迹（每 600 s 一点，共 348 点），可另行绘图。

---

## 5. 复现方式

```bash
python 论证_收缩与含水率自洽性.py          # 约 5 秒
python -u 论证_端面效应二维轴对称.py        # 两情形串行，约 65 分钟
```

- 两者均**只读**现有附件与结果文件，不修改任何求解或绘图代码
- 自带轻量 xlsx 解析（`zipfile` + `ElementTree`），不依赖 pandas / openpyxl
- 输出：`附件/附件3/论证_收缩自洽性.json`、`附件/附件3/论证_端面效应.json`
- **务必单进程串行运行**。两个情形并行跑会因 CPU 争抢使每步耗时从 121 ms 涨到 373 ms（3 倍），比串行还慢。（`--case both` 即为串行。）

### 二维求解器的实现要点

- 网格：$N_r\times N_z$ 单元中心有限体积，展平索引 $p=jN_r+i$，$\Delta r=\Delta z=0.05\ \mathrm{cm}$
- 离散：后向 Euler + Picard 迭代（$\omega=1.0$，收敛判据 $10^{-9}$），稀疏直接求解
- 边界：中心与对称面零通量；侧面 Robin；端面按情形取零通量或 Robin
- 表面半单元阻力与对流阻力串联，等效系数 $\beta_{\mathrm{eff}}=\left(1/\beta+\Delta r/(2\Gamma_s)\right)^{-1}$
- 界面物性取谐和平均
- 达标时刻用相邻两层线性插值，得到亚步分辨率的连续穿越时间

**关于 $\omega$**：本文主求解器使用 $\omega=0.6$ 欠松弛；本检验脚本改用 $\omega=1.0$。两者收敛到同一不动点（判据同为 $10^{-9}$），但 $\omega=1.0$ 的迭代次数约少 2.7 倍（7.8 次 vs 21.0 次），可显著缩短长周期二维计算时间。

**关于性能**：单步耗时随推进时间下降（初期边界变化剧烈，Picard 迭代多；后期准稳态，上一步的解是良好初值，迭代少）。本例中前 1000 步约 586 s，末 1000 步约 100 s，全程 6952 步约 26 min。

---

## 6. 论文手注意事项

1. **增补 C 的结论写法很关键。** 不要说"问题四的收缩假设被推翻"——它没有被推翻。假设 3 只约束内部速度场。正确的说法是：附件二的收缩由表面含水率驱动，若要把收缩内生化应以此为驱动量。

2. **表 10 的负值必须保留。** $\phi<0$ 是判定体积平均口径错误的直接证据，是本项检验最有力的一点，不能删。

3. **$\rho_s\approx742\ \mathrm{kg/m^3}$ 要注明是等效参数。** 理想收缩律假设无孔隙，反解出的 $\phi$ 把孔隙效应并入，不等于真实固体密度。不注明会被质疑数值偏低。

4. **判别力必须说清来自 $\phi$ 的恒定性而非 $R^2$。** 否则细心的评阅者会指出：既然 $R(t)$ 与含水率都单调，高 $R^2$ 是必然的，检验没有信息量。§7.1 末段已经写明这一点，不要删。

5. **前 6 h 的异常段要如实报告，不要略过。** 它是本项检验的第二个发现，说明附件二的收缩数据在初期含有水分流失之外的信息。

### 关于增补 D（端面效应）

6. **二维结果不能替代一维结果，用途不同。** 二维网格 $\Delta r=0.05\ \mathrm{cm}$ 比一维的 $0.00625\ \mathrm{cm}$ 粗 8 倍，情形 A 的 $t^*=57.9289\ \mathrm{h}$ 与一维高分辨率 $57.5224\ \mathrm{h}$ 相差 $1463.5\ \mathrm{s}$（$0.707\%$）。论文正文的烘干时长仍以一维结果为准；二维的用途**只是量化端面效应**。这一点必须写明，否则会与问题三的 57.52 h 矛盾。

7. **端面效应的量级要放在网格误差旁边讲。** 单独说"端面效应 7.6 s"没有说服力；说"端面效应 7.6 s，比同一组计算的径向网格误差 1463.5 s 小两个数量级"才说明问题——它意味着端面效应在当前精度下根本测不出来，一维假设不构成误差来源。

8. **"端面边界确实生效"的证据不要省。** 因为两种情形的达标时间只差 7.6 s，评阅者会怀疑是不是端面条件没接上。$t=600\ \mathrm s$ 的那组数（端面轴心由 2.550 降到 2.025，中部轴心两者相同）正是排除该疑虑的证据。

9. **机理解释要写成"端面面积 ≠ 有效干燥面积"。** 这是本项检验最有价值的一句话：题面提示端面占 7.41 %，容易被误当作"忽略端面会低估干燥面积 7.41 %"，从而怀疑一维模型低估了干燥速率。实际原因是水分自端面的渗透深度只有约 2 cm，而达标由距端面 12.5 cm 的中部轴心控制。

10. **$\omega$ 的差异可写可不写。** 若论文有篇幅讲数值实现，提一句"检验脚本用 $\omega=1.0$、主求解器用 $\omega=0.6$，二者收敛到同一不动点"是稳妥的；若没有篇幅，删掉也不影响结论。
