问题二建议采用“变物性、双向弱耦合的一维径向传热—传质模型”。它是问题一模型的自然升级，但必须从 \(t=0\) 重新计算整个 3 h，不能在 1800 s 处与问题一结果拼接。

## 一、问题二相比问题一改变了什么

| 项目 | 问题一 | 问题二 |
|---|---|---|
| 几何 | 固定半径圆柱 | 固定半径圆柱 |
| 密度 | 常数 | \(\rho=\rho(C)\) |
| 比热容 | 常数 | \(c_p=c_p(C)\) |
| 导热系数 | 常数 | \(k=k(C)\) |
| 扩散系数 | \(D=D(C)\) | \(D=D(C,T)\) |
| 热湿关系 | 数学上解耦 | 通过物性参数双向耦合 |
| 计算时长 | 1800 s | 10800 s |
| 边界数据 | 附件一前1800 s | 附件一前10800 s |

附件一完整覆盖了 0–3 h，采样间隔为 60 s，所以本问不需要进行边界外推。附件二的半径变化属于问题四，问题二仍取

\[
R=0.02\ \mathrm m.
\]

## 二、第一步：判断是否仍需空间分布模型

在初始状态 \(C_0=2.55\)、\(T_0=28^\circ\mathrm C\) 下，附录3给出

\[
\rho_0=650+128\times2.55=976.4\ \mathrm{kg/m^3},
\]

\[
c_{p,0}
=1450+2736\frac{2.55}{2.55+1}
\approx3415.30\ \mathrm{J/(kg\cdot K)},
\]

\[
k_0
=0.21+0.38\frac{2.55}{2.55+1}
\approx0.4830\ \mathrm{W/(m\cdot K)}.
\]

扩散系数中的温度必须使用开尔文温度：

\[
T_K=T+273.15.
\]

因此

\[
D_0
=2.4\times10^{-3}
\exp\left(-\frac{0.45}{2.55}\right)
\exp\left(-\frac{3850}{301.15}\right)
\approx5.64\times10^{-9}\ \mathrm{m^2/s}.
\]

取集中参数判据特征长度 \(L_c=R/2=0.01\ \mathrm m\)，若沿用问题一的

\[
h=25\ \mathrm{W/(m^2\cdot K)},\qquad
h_m=8\times10^{-7}\ \mathrm{m/s},
\]

则

\[
Bi_h=\frac{hL_c}{k_0}\approx0.518>0.1,
\]

\[
Bi_m=\frac{h_mL_c}{D_0}\approx1.418>0.1.
\]

因此，问题二仍然不能使用集中参数模型，必须求解径向空间分布。

初始热扩散率为

\[
\alpha_0=\frac{k_0}{\rho_0c_{p,0}}
\approx1.448\times10^{-7}\ \mathrm{m^2/s}.
\]

相应时间尺度为

\[
t_h\sim\frac{R^2}{\alpha_0}\approx2762\ \mathrm s,
\]

\[
t_m\sim\frac{R^2}{D_0}\approx7.09\times10^4\ \mathrm s.
\]

所以在 3 h 内，温度会逐渐接近烘房温度，但水分仍会保持明显的中心—表面梯度。

## 三、第二步：建立变物性控制方程

### 1. 温度场

采用局部表观物性模型：

\[
\boxed{
\rho(C)c_p(C)\frac{\partial T}{\partial t}
=
\frac1r\frac{\partial}{\partial r}
\left[
rk(C)\frac{\partial T}{\partial r}
\right]
}
\]

其中

\[
\rho(C)=650+128C,
\]

\[
c_p(C)=1450+2736\frac{C}{C+1},
\]

\[
k(C)=0.21+0.38\frac{C}{C+1}.
\]

这里必须保留通量散度形式，不能简单写成

\[
T_t=\alpha(C) \left(T_{rr}+\frac1rT_r\right),
\]

因为 \(k\) 随空间中的 \(C\) 变化，简单形式会遗漏导热系数梯度产生的通量变化。

### 2. 含水率场

\[
\boxed{
\frac{\partial C}{\partial t}
=
\frac1r\frac{\partial}{\partial r}
\left[
rD(C,T_K)\frac{\partial C}{\partial r}
\right]
}
\]

其中

\[
\boxed{
D(C,T_K)
=
2.4\times10^{-3}
\exp\left(-\frac{0.45}{C}\right)
\exp\left(-\frac{3850}{T_K}\right)
}
\]

且

\[
T_K=T+273.15.
\]

同样不能将它简写成 \(C_t=D\nabla^2C\)，因为 \(D\) 同时随温度和含水率变化。

## 四、为什么问题二是双向耦合

耦合关系可以表示为

\[
C
\longrightarrow
\rho(C),c_p(C),k(C)
\longrightarrow T,
\]

\[
T,C
\longrightarrow
D(C,T)
\longrightarrow C.
\]

也就是说：

- 含水率改变密度、比热和导热系数，进而影响温度场；
- 温度和含水率共同改变扩散系数，进而影响含水率场。

因此问题二不是问题一的两个独立方程，而是非线性双向弱耦合方程组。

之所以称为“弱耦合”，是因为耦合只通过物性系数实现，模型中暂未加入蒸发潜热、导湿温效应和相变源项。

## 五、初始条件和边界条件

圆心满足轴对称条件：

\[
\frac{\partial T}{\partial r}(0,t)=0,
\qquad
\frac{\partial C}{\partial r}(0,t)=0.
\]

表面对流换热条件为

\[
-k(C_s)\frac{\partial T}{\partial r}(R,t)
=
h[T_s-T_\infty(t)].
\]

表面对流传质条件为

\[
-D(C_s,T_s)\frac{\partial C}{\partial r}(R,t)
=
h_m[C_s-C_\infty(t)].
\]

初始条件仍为

\[
T(r,0)=28^\circ\mathrm C,
\qquad
C(r,0)=2.55\ \mathrm{kg/kg}.
\]

附件一的数据采用分段线性插值构造 \(T_\infty(t)\) 和 \(C_\infty(t)\)。

## 六、数值求解方案

推荐继续使用有限体积法，因为它适合处理圆柱坐标、变物性系数和表面对流通量。

空间取

\[
\Delta r=0.1\ \mathrm{cm}=0.001\ \mathrm m,
\]

得到 21 个径向节点；时间输出间隔取

\[
\Delta t=1\ \mathrm s.
\]

每个时间步采用联合 Picard 迭代：

1. 以上一时间层的 \(T^n,C^n\) 作为初始猜测；
2. 根据当前 \(C^{(m)},T^{(m)}\) 计算 \(\rho,c_p,k,D\)；
3. 对界面处的 \(k\) 和 \(D\) 采用调和平均；
4. 隐式求解温度三对角方程组；
5. 使用更新后的温度求解含水率三对角方程组；
6. 同时检查温度和含水率迭代误差；
7. 收敛后进入下一秒。

收敛条件可取

\[
\max_i\frac{|T_i^{(m+1)}-T_i^{(m)}|}
{1+|T_i^{(m+1)}|}
<10^{-9},
\]

\[
\max_i\frac{|C_i^{(m+1)}-C_i^{(m)}|}
{1+|C_i^{(m+1)}|}
<10^{-9}.
\]

如果含水率方程在后期出现迭代震荡，可以加入欠松弛，但不能依靠强制截断掩盖负值或不收敛。

## 七、输出要求

求解程序最终写入：

[附件/附件3/result2.xlsx](D:/黄洛霖的文件/竞赛/数模赛/CUMUCM2026A/附件/附件3/result2.xlsx)

其中：

- 时间为 \(1,2,\ldots,10800\ \mathrm s\)；
- 距离为 \(0,0.1,\ldots,2.0\ \mathrm{cm}\)；
- “温度”和“水分浓度”各一个工作表；
- 全部保留四位小数。

论文表3、表4提取

\[
t=0.5,1.0,1.5,2.0,2.5,3.0\ \mathrm h
\]

以及

\[
r=0,0.5,1.0,1.5,2.0\ \mathrm{cm}
\]

处的结果。

## 八、必须做的模型检验

后续代码需要至少验证：

- 温度始终处于初温与烘房最高温度之间；
- 含水率非负且不超过初始值；
- 温度一般满足表面高、中心低；
- 含水率一般满足中心高、表面低；
- \(\rho,c_p,k,D\) 在计算过程中始终为正；
- 加密到 40 个径向区间后，指定位置结果变化足够小；
- 将时间步长从 1 s 缩小到 0.5 s，结果基本稳定；
- 检查有限体积离散下的热量和水分通量收支；
- 对 \(h\) 和 \(h_m\) 做敏感性分析。

## 九、目前需要标注的不确定点

最重要的两个不确定点是：

1. 附录3没有重新给出 \(h\) 和 \(h_m\)。目前最自然的处理是沿用问题一的 \(25\ \mathrm{W/(m^2\cdot K)}\) 和 \(8\times10^{-7}\ \mathrm{m/s}\)，但最好在论文中明确说明，并做敏感性分析。

2. 当前温度方程是“局部表观物性模型”，没有蒸发潜热项。这不是完整的热湿耦合焓模型，但题面缺少汽化潜热、相变速率和吸附等温线参数，暂时无法可靠加入。

候选的高级模型是包含蒸发潜热的焓守恒模型，但在没有额外文献参数时，引入它反而会增加无法识别的参数，因此目前不建议作为主模型。

如果你认可“附录3从 \(t=0\) 统一使用，并沿用问题一的 \(h,h_m\)”这两个处理，我下一步就从控制体守恒开始，把问题二完整推导出来，然后生成论文正文、`问题2_求解.py`、`问题二_绘图.py`，图表只保留彩色 PNG。