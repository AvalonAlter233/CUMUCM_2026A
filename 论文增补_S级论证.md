# 论文增补：S 级论证插入指南

> 供论文手使用。本文档给出**改哪个文件的哪一节、原文定位句、以及可直接粘贴的正文**。
> 全部数值由 `论证_能量自洽性与全域最大值.py` 生成，可复现（见第 6 节）。
>
> 增补内容两部分：
> - **增补 A**：热湿耦合强度的量级判据 → 论证"为什么不加潜热项"（把免责声明变成硬论证）
> - **增补 B**：全域最大含水率位于轴心且单调不增 → 论证"为什么只盯轴心""为什么 $t_*$ 良定义"

---

## 1. 速览：一共改 7 处

| # | 文件 | 位置 | 操作 | 对应增补 |
|---|---|---|---|---|
| 1 | `问题二_论文内容.md` | §9 标题与节首 | 改标题 + **新增 §9.1** | A |
| 2 | `问题一_论文内容.md` | §2 假设第 6 条 | 压缩为一句 + 引用 | A |
| 3 | `问题一_论文内容.md` | §4.3 末段 | 压缩为一句 + 引用 | A |
| 4 | `问题二_论文内容.md` | §2 假设第 6 条 | 压缩为一句 + 引用 | A |
| 5 | `问题三_论文内容.md` | §4.5 末段 | **替换**为增补 B 正文 | B |
| 6 | `问题三_论文内容.md` | §7 结论第 2 条 | 改写 | B |
| 7 | `问题四_论文内容.md` | §6.1 末句 | **替换**为一行引用 | B |
| 8 | `问题四_论文内容.md` | §2 假设第 8 条 | 压缩为一句 + 引用 | A |

新增表格三张：**表 7**（能量收支，放问题二 §9.1）、**表 8**（表面平衡温度，放问题二 §9.1）、**表 9**（最大值性质核验，放问题三 §4.5）。

---

## 2. 增补 A：热湿耦合强度的量级判据

### 2.1 插入位置（改动 1）

**文件**：`问题二_论文内容.md`

**原 §9 标题**

```markdown
## 9. 边界参数敏感性与一维模型验证
```

**改为**

```markdown
## 9. 热湿耦合判据、边界参数敏感性与一维模型验证
```

**然后在原 §9 的第一段之前插入以下内容**（即 §9 标题下第一段变成 §9.1，原有两段依次下移为 §9.2、§9.3，并在原两段前各加小标题）：

```markdown
### 9.1 热湿耦合强度的量级判据

前两问的基准模型均未计入相变潜热，通常表述为"题目未提供有效汽化潜热参数"。此处给出更实质的依据：在题给对流系数 $(h,h_m)$ 下，潜热项与显热项的量级并不匹配。

模型在 $0\sim t$ 内由边界实际获得的热量为

$$
Q_{\mathrm{in}}(t)=\int_0^t h\left[T_\infty(\tau)-T_s(\tau)\right]\mathrm d\tau .
$$

该量由模型自身的 Robin 边界条件定义，不依赖焓的口径选择。需注意温度方程采用非守恒形式 $\rho(C)c_p(C)\,\partial T/\partial t=\nabla\!\cdot\!(k\nabla T)$，故 $\int\rho c_pT\,\mathrm dV$ 的差分不等于 $Q_{\mathrm{in}}$（见 §8.2）。模型预测的蒸发量所对应的潜热为

$$
Q_{\mathrm{lat}}(t)=\frac{R}{2}\left[\overline{\rho w}(0)-\overline{\rho w}(t)\right]\Delta H_{\mathrm{vap}},
\qquad
w=\frac{C}{1+C},
$$

其中 $\overline{\;\cdot\;}$ 为体积加权平均，$\Delta H_{\mathrm{vap}}$ 取 50 ℃ 水的汽化潜热 $2383\ \mathrm{kJ/kg}$。

模型在 $0\sim1800\ \mathrm s$ 内 $Q_{\mathrm{in}}=1.85\times10^5\ \mathrm{J/m^2}$，$Q_{\mathrm{lat}}=1.13\times10^6\ \mathrm{J/m^2}$；在 $0\sim10800\ \mathrm s$ 内 $Q_{\mathrm{in}}=6.67\times10^5\ \mathrm{J/m^2}$，$Q_{\mathrm{lat}}=5.32\times10^6\ \mathrm{J/m^2}$，即

$$
\frac{Q_{\mathrm{lat}}}{Q_{\mathrm{in}}}=6.1\ \ (0.5\ \mathrm h),
\qquad
8.0\ \ (3\ \mathrm h).
$$

由于题面未规定 $\rho(C)$ 与干基含水率的换算口径，表 7 同时报告三种口径下的结果，并给出边界供热绝对上界 $Q_{\mathrm{ub}}=h\int(T_\infty-28)\,\mathrm dt$（假设表面温度全程停留在初温 28 ℃）作为最宽松的对照基准。**在所有口径组合下 $Q_{\mathrm{lat}}>Q_{\mathrm{in}}$ 均成立，即使与绝对上界相比，比值仍不低于 1.07。**

### 表 7  能量收支量级对比（$0\sim1800\ \mathrm s$ 与 $0\sim10800\ \mathrm s$）

| 量 / 口径 | $0\sim1800\ \mathrm s$ | $0\sim10800\ \mathrm s$ |
|---|---:|---:|
| 边界热输入 $Q_{\mathrm{in}}$ / $\mathrm{J\cdot m^{-2}}$ | $1.85\times10^5$ | $6.67\times10^5$ |
| 边界供热绝对上界 $Q_{\mathrm{ub}}$ / $\mathrm{J\cdot m^{-2}}$ | $3.30\times10^5$ | $4.96\times10^6$ |
| 潜热需求 $Q_{\mathrm{lat}}$（严格口径）/ $\mathrm{J\cdot m^{-2}}$ | $1.13\times10^6$ | $5.32\times10^6$ |
| 潜热需求 $Q_{\mathrm{lat}}$（干基口径）/ $\mathrm{J\cdot m^{-2}}$ | $1.75\times10^6$ | $7.64\times10^6$ |
| 潜热需求 $Q_{\mathrm{lat}}$（上限口径）/ $\mathrm{J\cdot m^{-2}}$ | $4.13\times10^6$ | $1.81\times10^7$ |
| $Q_{\mathrm{lat}}/Q_{\mathrm{in}}$（严格口径） | $6.1$ | $8.0$ |
| $Q_{\mathrm{lat}}/Q_{\mathrm{ub}}$（严格口径） | $3.42$ | $1.07$ |

*注：严格口径取 $w=\rho(C)C/(1+C)$，不含待选参数；干基口径取 $\rho_d=\rho(C_0)/(1+C_0)=275.04\ \mathrm{kg/m^3}$；上限口径取 $\rho_d=\rho(0)=650.00\ \mathrm{kg/m^3}$。*

该量级差异说明：题给 $h$ 与 $h_m$ 无法同时作为同一物理过程的对流换热系数与传质系数。若按常规把相变潜热作为源项引入能量方程，表面能量平衡

$$
h\left[T_\infty(t)-T_s(t)\right]
=
j_s(t)\,\Delta H_{\mathrm{vap}}
$$

将要求表面温度显著低于烘房温度。以 6 h 为例，其平衡值落在 26.0 ℃ 至 39.9 ℃ 区间（随 $\rho_d$ 口径变化），较基准计算的约 50 ℃ 低 10～24 K；按附录三的 Arrhenius 因子 $e^{-3850/T_K}$ 折算，$D$ 将降至基准值的 $0.39\sim0.68$ 倍，达标时间随之显著改变。表 8 给出若干代表时刻的平衡值。

### 表 8  若引入潜热项的表面平衡温度与扩散系数衰减

| 时刻 / h | $C_s$ / $\mathrm{kg\cdot kg^{-1}}$ | $\rho_d=275.04$ 时 $T_s$ / ℃ | 对应 $D/D_{\text{基准}}$ | $\rho_d=650.00$ 时 $T_s$ / ℃ | 对应 $D/D_{\text{基准}}$ |
|---:|---:|---:|---:|---:|---:|
| 6 | 0.5333 | 39.86 | 0.680 | 26.04 | 0.385 |
| 12 | 0.1645 | 47.60 | 0.915 | 44.32 | 0.808 |
| 18 | 0.0845 | 49.27 | 0.974 | 48.29 | 0.938 |
| 24 | 0.0665 | 49.65 | 0.987 | 49.18 | 0.970 |

因此，本文从 $t=0$ 起按题面给出的扩散系数与对流系数分别求解温度场与含水率场，是在题给参数下唯一自洽的读法，而不是参数缺失导致的被迫简化。这也意味着本文的达标时间预测是题给经验公式的推论，不能直接解释为对真实药材烘干时长的物理预报。若后续获得自洽标定的 $(h,h_m)$ 对或有效汽化潜热，应据此重算；在此之前引入潜热项只会带来无法标定的能量失衡。

### 9.2 边界参数敏感性

（此处粘贴原有段落，文字不动）

### 9.3 一维模型验证

（此处粘贴原有段落，文字不动）
```

### 2.2 连带压缩（改动 2–5、8）

以下四处原文都是"因为题目没给参数所以不加"，现在统一压成一句并指向 §9.1。

**改动 2** — `问题一_论文内容.md` §2 假设第 6 条

原文：

```markdown
6. 题目未给出有效汽化潜热、干物质体积密度和热扩散系数，基准模型不加入潜热项和温度梯度导湿项。这里是参数不足下的模型简化，不表示这些效应在真实干燥中必然可以忽略。
```

改为：

```markdown
6. 基准模型不加入潜热项和温度梯度导湿项。这不是单纯的参数缺失下的省略：在题给 $(h,h_m)$ 下潜热项与显热项存在量级失配，依据见问题二 §9.1。
```

**改动 3** — `问题一_论文内容.md` §4.3 末段

原文：

```markdown
这是一项由题给本构关系和当前模型结构产生的解耦，不表示真实药材干燥不存在热湿相互作用。多孔介质干燥模型通常还可包含相变潜热和温度梯度导湿等耦合项[3-4]；本题因缺少相应参数未将其加入基准模型。第二问中 $D=D(C,T)$，且热物性随 $C$ 变化后，两场将通过物性参数发生双向联系。
```

改为：

```markdown
这是一项由题给本构关系和当前模型结构产生的解耦，不表示真实药材干燥不存在热湿相互作用。多孔介质干燥模型通常还可包含相变潜热和温度梯度导湿等耦合项[3-4]；本文不将其加入基准模型的量级依据见问题二 §9.1。第二问中 $D=D(C,T)$，且热物性随 $C$ 变化后，两场将通过物性参数发生双向联系。
```

**改动 4** — `问题二_论文内容.md` §2 假设第 6 条

原文：

```markdown
6. 基准模型仍不加入潜热项和温度梯度导湿项。多孔介质传热传质理论允许加入这些耦合机制[5-6]，但题目缺少有效汽化潜热、干物质体积密度等必要参数；本文不把参数不足表述为这些作用在真实过程中可以忽略。
```

改为：

```markdown
6. 基准模型仍不加入潜热项和温度梯度导湿项。多孔介质传热传质理论允许加入这些耦合机制[5-6]，但在题给对流系数下潜热项与显热项量级失配，引入后将与预热平衡设定矛盾，详见 §9.1。
```

**改动 5** — `问题四_论文内容.md` §2 假设第 8 条

原文：

```markdown
8. 不额外引入相变潜热、温度梯度导湿、应力和应变方程，以免增加无法由题给数据标定的参数。
```

改为：

```markdown
8. 不额外引入相变潜热、温度梯度导湿、应力和应变方程，以免增加无法由题给数据标定的参数；潜热项的量级依据见问题二 §9.1。
```

---

## 3. 增补 B：全域最大含水率位于轴心且随时间单调不增

### 3.1 插入位置（改动 6）

**文件**：`问题三_论文内容.md`

**定位**：§4.5 的末段，原文为

```markdown
本次基准计算的逐步监控显示，离散含水率剖面保持径向非增，所检查时间步的全域最大值均位于轴心。因此本算例的达标时刻由轴心含水率控制；这一结论是当前参数下的数值结果，不作为普遍性质外推。
```

**整段替换为**：

```markdown
数值监控显示全域最大含水率始终位于轴心。该现象并非仅对本算例成立，可在连续层面证明。

记 $M(t)=\max_{0\le r\le R}C(r,t)$。当 $t>0$ 时 $M(t)$ 不可能在 $r=R$ 处取得：若在 $(R,t_0)$ 处取得且 $M(t_0)>C_\infty(t_0)$，抛物型方程的 Hopf 引理给出 $\partial_rC(R,t_0)>0$，而表面条件 $-D\partial_rC=h_m(C-C_\infty)$ 在该点要求 $\partial_rC(R,t_0)<0$，二者矛盾；若 $M(t_0)\le C_\infty(t_0)$，则与 $C_0>\bar C_\infty\ge M(t_0)$ 及初始条件矛盾。再由均匀抛物方程的内部最大值原理排除内部极大点，并结合中心对称条件，$M(t)$ 只能位于 $r=0$。

进一步，在轴心附近写 $C(r,t)=C(0,t)+\tfrac12a(t)r^2+O(r^4)$，代入控制方程得

$$
\frac{\partial C}{\partial t}(0,t)=2D\big(C(0,t)\big)a(t).
$$

由于 $r=0$ 是空间极大点，$a(t)=C_{rr}(0,t)\le0$，故 $\partial_tM(t)\le0$，$M(t)$ 关于时间非增。

上述论证要求 $D$ 一致正定有界。附录三、四的扩散系数在 $C\to0^+$ 时渐近退化，故严格陈述限定在 $C$ 具有正下界的区间；基准计算中最小单元含水率为 $0.052646\ \mathrm{kg/kg}$，远大于零，该条件满足。问题四的参考域方程 $\partial_tC=\frac{1}{R^2(t)\xi}\partial_\xi(\xi D\partial_\xi C)$ 在 $R(t)\in[1.198,2.000]\ \mathrm{cm}$ 上结构相同且系数有界，同一结论适用。

该性质带来三点推论：（1）达标判定只需追踪轴心，但本文仍在每个时间步扫描全部单元与重构节点，用以核验数值格式保持该性质；（2）$t\mapsto M(t)$ 非增，故达标集合 $\{t:M(t)<0.15\}$ 是一个区间，"烘干所需时间"适定，$t_*$ 存在且唯一；（3）一旦达标将永远达标，无需追加时间步验证。

数值层面，上述性质得到完整核验（表 9）：固定半径算例的 3452 个 60 s 记录点全部由轴心控制，$M(t)$ 相对回升量为零；移动边界算例的 3067 个记录点全部由轴心控制，并进一步在 320 单元的内部细网格上逐时刻复核通过，最大相对回升量为 $2.2\times10^{-15}\ \mathrm{kg/kg}$，属浮点舍入量级。后向 Euler 配合调和平均界面导度给出严格对角占优的三对角 $M$ 矩阵，满足离散最大值原理，这是上述数值结果的格式依据。

### 表 9  全域最大含水率位置的数值核验

| 核验对象 | 记录点数 | 由轴心控制 | $M(t)$ 相对回升量 / $\mathrm{kg\cdot kg^{-1}}$ | 最小单元含水率 / $\mathrm{kg\cdot kg^{-1}}$ |
|---|---:|---:|---:|---:|
| 问题三 `result3.xlsx`（21 个输出位置） | 3452 | 3452 | $0$ | 0.052646 |
| 问题四 `result4.xlsx`（12 个固定物理位置） | 3067 | 3067 | $1.8\times10^{-15}$ | 0.093600 |
| 问题四内部细网格（320 个单元） | 3067 | 3067 | $2.2\times10^{-15}$ | 0.055667 |

*注：初期全场含水率接近初值时，若干相邻单元的浮点尾数差异会使逐点最大值出现在近轴心单元而非轴心本身。表中判定采用 $10^{-12}$ 容差；问题三有 2 个、问题四有 29～30 个此类并列点，全部位于 $t\le180\ \mathrm{s}$，不构成对上述性质的违反。*
```

### 3.2 连带改写（改动 7）

**文件**：`问题三_论文内容.md` §7 结论第 2 条

原文：

```markdown
2. **控制位置**：本次基准参数计算所检查的时间步中，轴心含水率均为全域最大值，因此该算例的烘干结束时刻由轴心位置控制；
```

改为：

```markdown
2. **控制位置**：由最大值原理可证全域最大值恒位于轴心且随时间非增（§4.5），因此烘干结束时刻由轴心位置控制，所定义的达标时刻 $t_*$ 存在且唯一；该结论已在 320 单元细网格上逐时刻核验；
```

### 3.3 连带改写（改动 8）

**文件**：`问题四_论文内容.md` §6.1「全域烘干结束时间」，定位句为该段**最后一句**。该段完整原文为

```markdown
附件三每 60 s 记录一次，故 `result4.xlsx` 中首次严格达标的记录点为 $t_{60}=51.1167\ \mathrm h$（184020 s）。该记录点的药材半径为 $1.2000\ \mathrm{cm}$，中心含水率为 $0.1499972281\ \mathrm{kg/kg}$，移动表面含水率为 $0.0526072221\ \mathrm{kg/kg}$。全过程数值检查表明含水率沿半径单调不增，全域最大值位于中心；这是本算例的计算结果，不作为一般性质外推。
```

**只替换最后一句**：

```markdown
全过程数值检查表明含水率沿半径单调不增，全域最大值位于中心。该性质已由最大值原理证明，并在 320 单元细网格上逐时刻核验通过，详见问题三 §4.5。
```

前面两句（关于 $t_{60}$、半径与中心含水率）保持不动。

**说明**：问题四 §5.3 只说明了 `result4.xlsx` 的列定义与内部细网格文件的用途，不含最大值位置的断言，无需改动。

---

## 4. 摘要可以新增的一句

如果摘要还有空间，建议加一句（把两处最弱的地方转成最强的地方）：

> 本文给出两个模型层面的论证：其一，在题给对流系数下蒸发所需潜热约为边界热输入的 6～8 倍，说明温度场与水分场按题面口径分别求解是参数自洽性的要求而非简化；其二，由最大值原理证明全域最大含水率恒位于轴心且随时间非增，使达标时刻良定义，并在 320 单元细网格上核验通过。

---

## 5. 图表清单

| 表号 | 内容 | 放置位置 |
|---|---|---|
| 表 7 | 能量收支量级对比 | 问题二 §9.1 |
| 表 8 | 若引入潜热项的表面平衡温度与扩散系数衰减 | 问题二 §9.1 |
| 表 9 | 全域最大含水率位置的数值核验 | 问题三 §4.5 |

无需新增图片。

---

## 6. 复现方式

```bash
cd <项目根目录>
python 论证_能量自洽性与全域最大值.py
```

- 只读以下文件，**不修改任何现有求解或绘图代码**：
  `附件/附件1.xlsx`、`附件/附件3/result2.xlsx`、`result3.xlsx`、`result4.xlsx`、`result4_internal_field.npz`
- 输出终端报表 + `附件/附件3/论证_diagnostics.json`
- 自带轻量 xlsx 解析（`zipfile` + `ElementTree`），**不依赖 pandas / openpyxl**
- 文档中表 7、表 8、表 9 的全部数值均取自该脚本输出

---

## 7. 论文手注意事项

1. **$Q_{\mathrm{in}}$ 不要写成焓差。** 温度方程是非守恒形式，$\int\rho c_pT\,\mathrm dV$ 的差分只有 $Q_{\mathrm{in}}$ 的 $0.48\sim0.71$ 倍，不能作为能量收支的左端。§8.2 已有同样的说明，两处口径要一致。

2. **$\Delta H_{\mathrm{vap}}$ 要注明取值条件。** 文中用的是 50 ℃ 水的汽化潜热 $2383\ \mathrm{kJ/kg}$；若审阅者按 100 ℃ 的 $2257\ \mathrm{kJ/kg}$ 复核，比值会小约 5%，不影响结论方向。建议在表 7 注释中写明。

3. **三种口径必须一起给。** 题面没有规定 $\rho(C)$ 与干基含水率的换算口径，只报单一数值会被质疑。表 7 的区间写法正是为了避免这一点。

4. **最大值原理的适用条件要保留。** 附录三、四的 $D$ 在 $C\to0^+$ 时退化，严格陈述必须限定在 $C$ 有正下界的区间。这句话不是可删的免责声明，而是证明成立的前提。

5. **表 9 的注必须保留。** 否则若有人用更严格的容差复核，会看到初期若干点最大值不在轴心，误以为是性质违反；注明是浮点并列即可。

6. **表 9 第三行（320 单元内部细网格）是新增的核验**，原文只在问题四 §5.3 声称"程序检查全部单元"而未给结果，现在补上了。建议保留，这比只查输出列更有说服力。
