下面是你们这道题的完整算法流程，可直接作为论文中的“总体算法流程”基础。

## 一、总体流程

```text
读取题目与附件
      ↓
数据清洗、单位统一、范围检查
      ↓
构造烘房边界 T∞(t)、C∞(t)
      ↓
建立圆柱形药材轴对称传热—传质模型
      ↓
有限体积离散
      ↓
隐式时间推进 + Picard非线性迭代
      ↓
重构题目要求的空间位置结果
      ↓
判断含水率是否达到0.15
      ↓
输出结果表、图像和诊断信息
      ↓
网格、时间步长、守恒性和敏感性检验
```

## 二、数据预处理

### 1. 附件一：烘房环境数据

读取烘房温度和水分浓度：

\[
T_\infty(t),\qquad C_\infty(t).
\]

在前 \(0\sim14400\ \mathrm{s}\) 内采用分段线性插值：

\[
T_\infty(t)=\operatorname{interp}(t,T_i),
\]

\[
C_\infty(t)=\operatorname{interp}(t,C_i).
\]

问题一、问题二直接使用题目给出的时间范围。

问题三、问题四超过 4 h 后，采用附件一最后 1 h 的均值作为长期稳定边界：

\[
T_\infty=\overline{T}_{\mathrm{last\,1h}},
\qquad
C_\infty=\overline{C}_{\mathrm{last\,1h}}.
\]

### 2. 附件二：药材半径数据

问题四读取半径变化：

\[
R(t_i).
\]

在观测点之间采用分段线性插值：

\[
R(t)=R_i+
\frac{R_{i+1}-R_i}{t_{i+1}-t_i}(t-t_i).
\]

超过附件二的观测范围后保持最后一个半径不变。

### 3. 数据检查

主要检查：

- 时间是否严格递增；
- 半径是否单调不增；
- 温度和含水率是否存在缺失值；
- 单位是否统一为 SI 制；
- 所有扩散系数、热物性是否为正。

## 三、统一物理模型

药材视为圆柱形多孔介质，采用一维轴对称径向模型。

固定半径时：

\[
0\le r\le R_0,\qquad R_0=0.02\ \mathrm m.
\]

中心对称边界：

\[
\frac{\partial T}{\partial r}(0,t)=0,
\qquad
\frac{\partial C}{\partial r}(0,t)=0.
\]

表面采用 Robin 边界：

\[
-k\frac{\partial T}{\partial r}
=h(T_s-T_\infty),
\]

\[
-D\frac{\partial C}{\partial r}
=h_m(C_s-C_\infty).
\]

初始条件为：

\[
T(r,0)=28^\circ\mathrm C,
\qquad
C(r,0)=2.55\ \mathrm{kg/kg}.
\]

温度方程：

\[
\rho(C)c_p(C)\frac{\partial T}{\partial t}
=
\frac1r\frac{\partial}{\partial r}
\left(rk(C)\frac{\partial T}{\partial r}\right).
\]

含水率方程：

\[
\frac{\partial C}{\partial t}
=
\frac1r\frac{\partial}{\partial r}
\left(rD(C,T)\frac{\partial C}{\partial r}\right).
\]

其中，所有空间传输项均保留为通量散度形式。

## 四、数值离散算法

### 1. 有限体积离散

将径向区域划分为同心柱壳控制体。

第 \(i\) 个控制体的体积因子为：

\[
V_i=\frac12(r_{i+1/2}^2-r_{i-1/2}^2).
\]

对传热、传质方程分别进行积分，得到守恒型离散方程。

界面传输系数采用调和平均：

\[
D_{i+1/2}
=
\frac{2D_iD_{i+1}}{D_i+D_{i+1}},
\]

导热系数同理。

### 2. 表面半单元处理

由于最外层单元中心不位于真实表面，需要同时考虑：

- 单元中心到真实表面的内部传递阻力；
- 真实表面到烘房环境的对流阻力。

等效传递系数为：

\[
\beta_{\mathrm{eff}}
=
\left(
\frac1\beta+
\frac{\Delta r}{2\Gamma_s}
\right)^{-1}.
\]

这样可以避免把最后一个单元中心值直接当成表面值。

### 3. 隐式时间推进

采用后向 Euler 格式：

\[
\frac{u_i^{n+1}-u_i^n}{\Delta t}
=
\mathcal L(u^{n+1}).
\]

这样可以提高长时间计算过程中的稳定性。

### 4. Picard 非线性迭代

由于物性随 \(C,T\) 变化，每个时间步需要迭代：

```text
给定上一个时间层 Tn、Cn
      ↓
初始化 T(n+1)、C(n+1)
      ↓
根据当前 C、T 更新 ρ、cp、k、D
      ↓
求解温度方程
      ↓
求解含水率方程
      ↓
欠松弛更新 T、C
      ↓
判断是否收敛
      ↓
未收敛则重复上述过程
```

收敛条件为：

\[
\max_i
\frac{|u_i^{(k+1)}-u_i^{(k)}|}
{1+|u_i^{(k+1)}|}
<10^{-9}.
\]

三对角线性方程组采用 Thomas 算法求解。

## 五、问题一算法流程

问题一使用附录二参数，半径固定。

### 计算设置

- 计算时间：\(0\sim1800\ \mathrm{s}\)；
- 时间步长：\(1\ \mathrm{s}\)；
- 半径：\(R=2\ \mathrm{cm}\)；
- 温度方程与含水率方程相互解耦；
- \(D=D(C)\)。

### 具体流程

```text
读取附件一前1800 s的烘房温度和水分浓度
      ↓
建立固定半径圆柱径向网格
      ↓
分别求解温度场和含水率场
      ↓
每隔1 s保存一次结果
      ↓
将结果重构到0、0.1、…、2.0 cm位置
      ↓
输出 result1.xlsx
```

问题一的温度和含水率方程可以分别求解，因为题目给出的参数结构没有形成显式交叉耦合。

## 六、问题二算法流程

问题二使用附录三变物性公式，半径仍固定。

\[
\rho=\rho(C),\quad
c_p=c_p(C),\quad
k=k(C),
\]

\[
D=D(C,T).
\]

此时温度场和含水率场通过物性参数发生双向耦合。

### 计算设置

- 计算时间：\(0\sim10800\ \mathrm{s}\)；
- 半径固定为 \(2\ \mathrm{cm}\)；
- 使用附录三全部经验公式；
- 每个时间步执行 Picard 迭代；
- 每隔 \(1\ \mathrm{s}\) 输出结果。

### 具体流程

```text
读取附件一0~3 h环境数据
      ↓
初始化 T=28℃、C=2.55
      ↓
根据当前 C、T计算ρ、cp、k、D
      ↓
隐式求解温度场
      ↓
隐式求解含水率场
      ↓
Picard迭代至收敛
      ↓
输出1 s × 0.1 cm结果
      ↓
输出 result2.xlsx
```

## 七、问题三算法流程

问题三沿用问题二的变物性模型，但将计算时间延长到药材全域达标。

### 1. 边界延拓

前 4 h：

\[
T_\infty(t),C_\infty(t)
\]

使用附件一插值。

4 h 后：

\[
T_\infty(t)=\overline T_\infty,
\qquad
C_\infty(t)=\overline C_\infty.
\]

### 2. 内部计算与规范输出分离

内部采用较细网格计算：

\[
N=320,\qquad \Delta t=30\ \mathrm s.
\]

题目要求每 60 s 输出，因此：

- 内部计算步长：30 s；
- 结果输出步长：60 s；
- 输出空间位置：每隔 0.1 cm。

### 3. 达标判定

每个时间层计算全域最大含水率：

\[
C_{\max}(t)
=
\max_{0\le r\le R}C(r,t).
\]

当：

\[
C_{\max}(t^n)\ge0.15,
\qquad
C_{\max}(t^{n+1})<0.15
\]

时，通过线性插值得到连续达标时间：

\[
t_*=
t_n+
\frac{C_{\max}(t_n)-0.15}
{C_{\max}(t_n)-C_{\max}(t_{n+1})}
\Delta t.
\]

同时记录第一个满足：

\[
C_{\max}(60k)<0.15
\]

的 60 s 输出时刻 \(t_{60}\)。

### 4. 问题三流程

```text
读取附件一并建立长期边界
      ↓
采用附录三变物性模型
      ↓
初始化温度场和含水率场
      ↓
推进30 s时间步
      ↓
Picard迭代求解 T、C
      ↓
扫描全域最大含水率
      ↓
判断是否低于0.15
      ↓
若未达标，继续计算
      ↓
若达标，插值得到连续时间 t*
      ↓
每60 s保存规范输出
      ↓
输出 result3.xlsx
```

### 5. 问题三验证

包括：

- 空间网格加密；
- 时间步长加密；
- 含水率通量收支；
- 长期温度平台敏感性；
- 长期水分边界敏感性；
- 中心位置是否始终控制全域达标。

## 八、问题四算法流程

问题四在问题三的基础上进行两项改变：

1. 使用附录四物性；
2. 使用附件二给出的移动半径。

### 1. 移动半径

药材实际区域变为：

\[
0\le r\le R(t).
\]

内部采用材料参考坐标：

\[
\xi=\frac r{R(t)},
\qquad 0\le \xi\le1.
\]

比例收缩速度为：

\[
v(r,t)=\frac{\dot R(t)}{R(t)}r.
\]

由于参考坐标随材料一起移动，温度和含水率方程变为：

\[
\rho c_p\frac{\partial T}{\partial t}
=
\frac{1}{R^2(t)\xi}
\frac{\partial}{\partial \xi}
\left(
\xi k\frac{\partial T}{\partial \xi}
\right),
\]

\[
\frac{\partial C}{\partial t}
=
\frac{1}{R^2(t)\xi}
\frac{\partial}{\partial \xi}
\left(
\xi D\frac{\partial C}{\partial \xi}
\right).
\]

这里不再额外添加对流项，因为材料运动已经包含在坐标变换中。

### 2. 问题四具体流程

```text
读取附件一：烘房温度和水分边界
      ↓
读取附件二：药材半径 R(t)
      ↓
建立归一化参考域 0≤ξ≤1
      ↓
采用附录四物性公式
      ↓
每个时间步更新当前半径 R(t)
      ↓
在参考域内组装有限体积方程
      ↓
Picard迭代求解温度和含水率
      ↓
将参考域结果映射回实际物理位置
      ↓
同时重构移动表面含水率
      ↓
扫描全域最大含水率
      ↓
判断是否达到0.15
      ↓
输出 result4.xlsx
```

### 3. 结果重构

题目要求的是固定物理位置，因此将：

\[
r_j=0,0.1,\ldots,1.1\ \mathrm{cm}
\]

映射为：

\[
\xi_j(t)=\frac{r_j}{R(t)}.
\]

药材表面位置始终取：

\[
\xi=1.
\]

因此问题四同时输出：

- 固定物理位置含水率；
- 当前移动表面含水率；
- 当前半径；
- 全域最大含水率。

## 九、四问之间的继承关系

```text
问题一
固定半径 + 常物性 + 基础传热传质模型
      ↓
问题二
固定半径 + 附录三变物性 + 温度—含水率耦合
      ↓
问题三
问题二模型 + 长周期边界延拓 + 全域达标判定
      ↓
问题四
问题三边界处理 + 附录四物性 + 实测移动半径
      ↓
归一化参考域移动边界模型
```

## 十、最终统一验证流程

每一问计算完成后都进行以下检查：

```text
检查温度、含水率是否为有限值
      ↓
检查物性参数是否为正
      ↓
检查含水率是否出现负值
      ↓
检查径向含水率是否保持合理的非增趋势
      ↓
检查中心是否为全域最大含水率
      ↓
检查网格和时间步长收敛性
      ↓
检查边界通量与内部存量变化是否守恒
      ↓
问题四额外检查固定半径退化一致性
```

最终输出包括：

- `result1.xlsx`
- `result2.xlsx`
- `result3.xlsx`
- `result4.xlsx`
- 各问题温度场、含水率场和时空分布图；
- 达标时间；
- 网格、时间步长、守恒和敏感性诊断结果。